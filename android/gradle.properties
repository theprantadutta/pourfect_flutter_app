org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# Kotlin incremental compilation writes .tab cache files that Windows keeps
# locked (indexer/AV), so :firebase_core:compileDebugKotlin fails with
# "Could not close incremental caches". Disabling incremental compilation
# costs a few seconds per clean build and makes the build deterministic.
kotlin.incremental=false
